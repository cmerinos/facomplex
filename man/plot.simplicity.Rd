% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.simplicity.R
\name{plot.simplicity}
\alias{plot.simplicity}
\title{Plot Simplicity Index Values}
\usage{
\method{plot}{simplicity}(
  data,
  item.col = "Item",
  value.col = "Coefficient",
  sort.items = c("ascending", "none", "descending"),
  reverse.items = FALSE,
  theme = c("light", "classic", "minimal"),
  title = "Simplicity Index by Item",
  bar.color = "blue",
  threshold.line = NULL,
  threshold.color = "red"
)
}
\arguments{
\item{data}{A data frame with item labels and simplicity or complexity values.}

\item{item.col}{Character. Name of the column with item labels. Default is \code{"Item"}.}

\item{value.col}{Character. Name of the column with the simplicity or complexity values. This argument is required.}

\item{sort.items}{Character. Sorting order of items: \code{"none"}, \code{"ascending"}, or \code{"descending"}. Default is \code{"ascending"}.}

\item{reverse.items}{Logical. If \code{TRUE}, reverses the order of items on the Y axis (top to bottom). Default is \code{FALSE}.}

\item{theme}{Character. ggplot2 theme to use: \code{"light"}, \code{"classic"}, or \code{"minimal"}. Default is \code{"light"}.}

\item{title}{Title of the plot. Default is \code{"Simplicity Index by Item"}.}

\item{bar.color}{Fill color for the bars. Default is \code{"blue"}.}

\item{threshold.line}{Optional numeric value. If specified, a horizontal dashed reference line is drawn at this threshold.}

\item{threshold.color}{Color for the threshold line and label. Default is \code{"red"}.}
}
\value{
A horizontal ggplot2 bar plot of item-level values.
}
\description{
Creates a horizontal bar plot of item-level simplicity or complexity values (e.g., from FSI, BSI, Hofmann indices).
The user must specify the column name that contains the coefficient values (e.g., "FSI_i", "Complexity", etc.).
}
\examples{
# Example using FSI output:
FSIout <- FSI(ex1_fl,  
              items_target = list(F1 = c(1, 2, 3, 4, 5, 6),
                                  F2 = c(7, 8, 9),
                                  F3 = c(10, 11, 12)))

# Basic use (value.col is required)
plot.simplicity(
  data = FSIout$FSI_i,
  item.col = "Items",
  value.col = "FSI_i"
)

# Customizing options:
plot.simplicity(
  data = FSIout$FSI_i,
  item.col = "Items",
  value.col = "FSI_i",
  sort.items = "none",
  reverse.items = TRUE,
  theme = "classic",
  bar.color = "darkgreen",
  threshold.line = 0.90
)

# ❌ This will trigger an error if value.col is missing:
\dontrun{
plot.simplicity(data = FSIout$FSI_i)  # Error: 'value.col' is required
}
}
